## Data storage

- Save the Spire targets stream as daily gzip-compressed JSONL files.
- Keep compressed JSONL as the raw archival format for now.
- Convert the archived data to Avro or Parquet later for schema-aware analytics.
